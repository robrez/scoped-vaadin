Index: test/keyboard-navigation.test.js
===================================================================
--- test/keyboard-navigation.test.js
+++ test/keyboard-navigation.test.js
@@ -28,12 +28,12 @@
   });
 
   beforeEach(async () => {
-    uploadElement = fixtureSync(`<vaadin-upload></vaadin-upload>`);
+    uploadElement = fixtureSync(`<vaadin23-upload></vaadin23-upload>`);
     uploadElement.files = [FAKE_FILE];
 
     await nextRender();
 
-    fileElement = uploadElement.shadowRoot.querySelector('vaadin-upload-file');
+    fileElement = uploadElement.shadowRoot.querySelector('vaadin23-upload-file');
   });
 
   afterEach(() => {
