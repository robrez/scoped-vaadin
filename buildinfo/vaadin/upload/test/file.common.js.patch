Index: test/file.common.js
===================================================================
--- test/file.common.js
+++ test/file.common.js
@@ -3,11 +3,11 @@
 import { sendKeys } from '@web/test-runner-commands';
 import { createFile } from './helpers.js';
 
-describe('<vaadin-upload-file> element', () => {
+describe('<vaadin24-upload-file> element', () => {
   let fileElement, fileObject;
 
   beforeEach(async () => {
-    fileElement = fixtureSync(`<vaadin-upload-file></vaadin-upload-file>`);
+    fileElement = fixtureSync(`<vaadin24-upload-file></vaadin24-upload-file>`);
     fileElement.i18n = {
       file: {
         start: 'Start',
