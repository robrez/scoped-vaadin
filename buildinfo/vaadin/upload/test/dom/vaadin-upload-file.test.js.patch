Index: test/dom/vaadin-upload-file.test.js
===================================================================
--- test/dom/vaadin-upload-file.test.js
+++ test/dom/vaadin-upload-file.test.js
@@ -2,11 +2,11 @@
 import { fixtureSync } from '@vaadin/testing-helpers';
 import '../../src/vaadin-upload-file.js';
 
-describe('vaadin-upload-file', () => {
+describe('vaadin24-upload-file', () => {
   let uploadFile;
 
   beforeEach(() => {
-    uploadFile = fixtureSync('<vaadin-upload-file></vaadin-upload-file>');
+    uploadFile = fixtureSync('<vaadin24-upload-file></vaadin24-upload-file>');
     uploadFile.file = {
       name: 'Workflow.pdf',
       progress: 60,
@@ -17,7 +17,7 @@
   describe('shadow', () => {
     const SNAPSHOT_CONFIG = {
       // Exclude inline style as we are not testing
-      // the `vaadin-progress-bar` internal logic.
+      // the `vaadin24-progress-bar` internal logic.
       ignoreAttributes: ['style'],
     };
 
