Index: test/visual/material/message.test.js
===================================================================
--- test/visual/material/message.test.js
+++ test/visual/material/message.test.js
@@ -20,7 +20,7 @@
           div = document.createElement('div');
           div.style.padding = '10px';
           element = fixtureSync(
-            `<vaadin-message>There is no real ending. It's just the place where you stop the story.</vaadin-message>`,
+            `<vaadin24-message>There is no real ending. It's just the place where you stop the story.</vaadin24-message>`,
             div,
           );
         });
