Index: src/lit/column-renderer-directives.d.ts
===================================================================
--- src/lit/column-renderer-directives.d.ts
+++ src/lit/column-renderer-directives.d.ts
@@ -6,8 +6,8 @@
 /* eslint-disable max-classes-per-file */
 import type { TemplateResult } from 'lit';
 import type { DirectiveResult } from 'lit/directive';
-import type { LitRenderer } from '@vaadin/lit-renderer';
-import { LitRendererDirective } from '@vaadin/lit-renderer';
+import type { LitRenderer } from '@scoped-vaadin/lit-renderer';
+import { LitRendererDirective } from '@scoped-vaadin/lit-renderer';
 import type { GridItemModel } from '../vaadin-grid.js';
 import type { GridColumn } from '../vaadin-grid-column.js';
 
@@ -76,9 +76,9 @@
  *
  * Example of usage:
  * ```js
- * `<vaadin-grid-column
+ * `<vaadin23-grid-column
  *   ${columnBodyRenderer((item, model, column) => html`...`)}
- * ></vaadin-grid-column>`
+ * ></vaadin23-grid-column>`
  * ```
  *
  * @param renderer the renderer callback.
@@ -107,9 +107,9 @@
  *
  * Example of usage:
  * ```js
- * `<vaadin-grid-column
+ * `<vaadin23-grid-column
  *   ${columnHeaderRenderer((column) => html`...`)}
- * ></vaadin-grid-column>`
+ * ></vaadin23-grid-column>`
  * ```
  *
  * @param renderer the renderer callback.
@@ -138,9 +138,9 @@
  *
  * Example of usage:
  * ```js
- * `<vaadin-grid-column
+ * `<vaadin23-grid-column
  *   ${columnFooterRenderer((column) => html`...`)}
- * ></vaadin-grid-column>`
+ * ></vaadin23-grid-column>`
  * ```
  *
  * @param renderer the renderer callback.
